services:
  mysql:
    image: mysql:8.4
    container_name: estacionamento-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: estacionamento
      MYSQL_USER: dev
      MYSQL_PASSWORD: dev123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/init:/docker-entrypoint-initdb.d

  backend-dev:
    profiles: ["dev"]
    container_name: estacionamento-backend-dev
    build:
      context: ./estacionamento-backend-mobile2
      target: dev
    environment:
      PORT: 3000
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: estacionamento
      DB_USER: dev
      DB_PASSWORD: dev123
    volumes:
      - ./estacionamento-backend-mobile2:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    depends_on:
      - mysql

  frontend-dev:
    profiles: ["dev"]
    container_name: estacionamento-frontend-dev
    build:
      context: ./estacionamento-frontend-mobile2
      target: dev
    environment:
      API_BASE_URL: http://backend-dev:3000
      EXPO_PUBLIC_API_BASE_URL: http://backend-dev:3000
      EXPO_NO_TELEMETRY: "1"
    volumes:
      - ./estacionamento-frontend-mobile2:/app
      - /app/node_modules
    ports:
      - "8081:8081"
    depends_on:
      - backend-dev

  backend-prod:
    profiles: ["prod"]
    container_name: estacionamento-backend
    build:
      context: ./estacionamento-backend-mobile2
      target: prod
    environment:
      PORT: 3000
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: estacionamento
      DB_USER: dev
      DB_PASSWORD: dev123
    ports:
      - "3000:3000"
    depends_on:
      - mysql

  frontend-prod:
    profiles: ["prod"]
    container_name: estacionamento-frontend
    build:
      context: ./estacionamento-frontend-mobile2
      target: prod
      args:
        EXPO_PUBLIC_API_BASE_URL: http://10.1.0.4:3000
    ports:
      - "8080:80"
    depends_on:
      - backend-prod

volumes:
  mysql_data:
